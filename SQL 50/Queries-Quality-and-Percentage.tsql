1/* Write your T-SQL query statement below */
2select query_name, cast(avg(rating * 1.0/nullif(position, 0))as decimal (10, 2)) as quality, round(count(case when rating < 3 then 1 end) * 100.0 /count(*), 2) as poor_query_percentage
3from Queries
4group by query_name
5